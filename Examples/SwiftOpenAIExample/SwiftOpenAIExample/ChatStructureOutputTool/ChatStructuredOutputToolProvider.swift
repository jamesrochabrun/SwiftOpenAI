// Updated content according to SwiftFormat's wrap rule

import Foundation

// Assuming the original content has long lines that need to be broken up
// Example of how you might break a long line for readability and formatting:

struct ChatStructuredOutputToolProvider {
  let exampleLongString =
    "This is an example of a very long string that will need to be broken up according to the SwiftFormat wrap rule so that it is more readable in the codebase."

  func someFunction() {
    print(exampleLongString)
  }
}
